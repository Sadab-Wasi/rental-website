<div class="tm-welcome-section <% if (pageTitle != 'Home') {%>section-sm<% } %>">
    <div class="container tm-navbar-container">
        <div class="row">
            <div class="col-xl-12">
                <nav class="navbar navbar-expand-sm">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a href="/admin" class="nav-link tm-nav-link tm-text-white <% if (pageTitle === '') {%>active<% } %>">Admin</a>
                        </li>
                    </ul>
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item active">
                            <a href="/" class="nav-link tm-nav-link tm-text-white <% if (pageTitle === 'Home') {%>active<% } %>">Home</a>
                        </li>
                        <li class="nav-item">
                            <a href="/shop" class="nav-link tm-nav-link tm-text-white <% if (pageTitle === 'Shop') {%>active<% } %>">Shop</a>
                        </li>
                        <li class="nav-item">
                            <a href="/cart" class="nav-link tm-nav-link tm-text-white <% if (pageTitle === 'Cart') {%>active<% } %>">Cart</a>
                        </li>
                    </ul>
                </nav>
            </div>
        </div>
    </div>

    <div class="container text-center tm-welcome-container <% if (pageTitle !== 'Home') {%>welcome-container-sm<% } %> " >
        <div class="tm-welcome">
            <i class="fas tm-fa-big fa-shopping-cart tm-fa-mb-big" <% if (pageTitle != 'Home') {%>style="display:none"<% } %>></i>
            <h1 class="text-uppercase mb-1 tm-site-name">SnapShop</h1>
            <p class="tm-site-description" <% if (pageTitle != 'Home') {%>style="display:none"<% } %>>Purchase your favourite today</p>
        </div>
    </div>
</div>
